#!/bin/bash

# Check if the PYCODE environment variable is set
if [ -z "$PYCODE" ]; then
  echo "PYCODE environment variable is not set. Please set it to the Python code you want to execute."
  exit 1
fi

# Use the Python interpreter to execute the code stored in PYCODE
python3 -c "$PYCODE"
